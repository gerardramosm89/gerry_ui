import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class TestResource extends Component {
  render() {
    return (
      <div>
        This is the protected resources!
      </div>
    );
  }
}

export default TestResource;